# Intent OS Reference Runtime — Phase 0 Dependencies
#
# Core: PyYAML for Manifest parsing
# Optional: openai for OpenAI adapter, anthropic for Anthropic adapter
# At least one adapter package is required for execution

# Core
pyyaml>=6.0

# Runtime Adapters (install at least one)
openai>=1.0.0
anthropic>=0.30.0

# Development
pytest>=8.0
